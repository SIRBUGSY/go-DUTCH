# Claude Code Guide

Read `AGENTS.md` and `PROJECT_CONTEXT.md` before making changes.

`AGENTS.md` contains the shared workflow for coding agents.
`PROJECT_CONTEXT.md` contains the detailed project knowledge and is the source
of truth for implementation decisions.

Do not commit, push, or deploy unless the user explicitly asks.
